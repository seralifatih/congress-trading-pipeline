{
  "actorSpecification": 1,
  "name": "house-trading-pipeline",
  "title": "U.S. House Trading Pipeline",
  "description": "Fetches U.S. House Periodic Transaction Reports from the official disclosures-clerk.house.gov ZIP archive, parses the per-filing PDFs, and emits a clean transaction dataset. Public domain data, no third-party vendors.",
  "version": "0.1.0",
  "buildTag": "latest",
  "dockerfile": "../Dockerfile.house",
  "input": {
    "schemaVersion": 1,
    "title": "House Trading Pipeline Input",
    "description": "Optional overrides for the pipeline run.",
    "type": "object",
    "properties": {
      "fetchDaysBack": {
        "title": "Days back to fetch",
        "type": "integer",
        "description": "Rolling window of PTRs to fetch (default 90).",
        "default": 90,
        "minimum": 1,
        "maximum": 365,
        "editor": "number"
      },
      "fromDate": {
        "title": "From date (YYYY-MM-DD)",
        "type": "string",
        "description": "Explicit start date. Overrides fetchDaysBack if set.",
        "editor": "textfield",
        "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
      },
      "toDate": {
        "title": "To date (YYYY-MM-DD)",
        "type": "string",
        "description": "Explicit end date. Defaults to today.",
        "editor": "textfield",
        "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
      }
    }
  },
  "storages": {
    "dataset": "./dataset_schema.json"
  },
  "output": "./output_schema.json"
}
