{
  "actorSpecification": 1,
  "fields": {
    "type": "object",
    "required": [
      "politician",
      "transaction_date",
      "filing_date",
      "asset_name",
      "asset_type",
      "type",
      "amount_min",
      "owner",
      "id",
      "source_id",
      "content_hash"
    ],
    "properties": {
      "id": {
        "type": "string",
        "title": "ID",
        "description": "SHA-256 of (politician, transaction_date, asset_name, amount_min, amount_max, source_id). Unique per row — changes if source_id changes.",
        "example": "057cfd6485562a167c140ba950f5835cccf3bbb5fdaf55163f08c37c3cdcd372"
      },
      "source_id": {
        "type": "string",
        "title": "Source document ID",
        "description": "The source PTR's DocID plus the row's ordinal within it (house_<DocID>_<row_index>). Identifies exactly which document and line produced this row.",
        "example": "house_20034201_0"
      },
      "content_hash": {
        "type": "string",
        "title": "Content hash",
        "description": "SHA-256 of (politician, transaction_date, asset_name, type, amount_min, amount_max, owner) — source_id excluded. Rows sharing this within the same document are legitimate separate tranches; rows sharing it across different documents are the same real-world transaction reported twice. Never used to drop or merge rows — see actor README.",
        "example": "7c2e5b8d4f6a0c9e3b7d1fa3f9c1e2b8d47f60a1c5e93b2d8f7a4c6e0b1d9f3a"
      },
      "filing_type": {
        "type": ["string", "null"],
        "title": "Filing type",
        "description": "Read from the PTR's own per-row \"Filing Status: New/Amended\" line. Null only when that line is missing — never inferred from duplicate documents.",
        "enum": ["original", "amendment", null],
        "example": "original"
      },
      "politician": {
        "type": "string",
        "title": "Politician",
        "description": "Filer name as it appears on the Senate disclosure.",
        "example": "Tommy Tuberville"
      },
      "transaction_date": {
        "type": "string",
        "title": "Transaction date",
        "description": "ISO date (YYYY-MM-DD) when the trade was executed.",
        "example": "2026-04-15"
      },
      "filing_date": {
        "type": "string",
        "title": "Filing date",
        "description": "ISO date (YYYY-MM-DD) when the PTR was filed with the Senate.",
        "example": "2026-04-20"
      },
      "ticker": {
        "type": ["string", "null"],
        "title": "Ticker",
        "description": "Stock ticker. Null for bonds, municipal securities, structured notes, and other non-equity instruments.",
        "example": "NVDA"
      },
      "asset_name": {
        "type": "string",
        "title": "Asset name",
        "description": "Full asset description as reported on the PTR.",
        "example": "NVIDIA Corporation - Common Stock"
      },
      "asset_type": {
        "type": "string",
        "title": "Asset type",
        "description": "Senate EFD asset classification.",
        "example": "Stock"
      },
      "type": {
        "type": "string",
        "title": "Trade type",
        "description": "Normalized to 'buy' (Purchase) or 'sell' (Sale Full / Sale Partial).",
        "enum": ["buy", "sell"],
        "example": "buy"
      },
      "amount_min": {
        "type": "integer",
        "title": "Amount lower bound",
        "description": "Lower bound of the reported amount range, in USD.",
        "example": 1001
      },
      "amount_max": {
        "type": ["integer", "null"],
        "title": "Amount upper bound",
        "description": "Upper bound of the reported amount range, in USD. Null for unbounded 'Over $X' brackets.",
        "example": 15000
      },
      "owner": {
        "type": "string",
        "title": "Owner",
        "description": "Account owner classification per STOCK Act.",
        "enum": ["self", "joint", "spouse", "child"],
        "example": "self"
      }
    }
  },
  "views": {
    "overview": {
      "title": "Transaction overview",
      "description": "Compact view — politician, ticker, type, amounts, dates.",
      "transformation": {
        "fields": [
          "politician",
          "transaction_date",
          "filing_date",
          "ticker",
          "type",
          "amount_min",
          "amount_max",
          "owner"
        ]
      },
      "display": {
        "component": "table",
        "properties": {
          "politician": { "label": "Politician", "format": "text" },
          "transaction_date": { "label": "Trade date", "format": "date" },
          "filing_date": { "label": "Filed", "format": "date" },
          "ticker": { "label": "Ticker", "format": "text" },
          "type": { "label": "Type", "format": "text" },
          "amount_min": { "label": "Min $", "format": "number" },
          "amount_max": { "label": "Max $", "format": "number" },
          "owner": { "label": "Owner", "format": "text" }
        }
      }
    },
    "buys_only": {
      "title": "Purchases only",
      "description": "Filter to only buy-side transactions.",
      "transformation": {
        "fields": [
          "politician",
          "transaction_date",
          "ticker",
          "asset_name",
          "amount_min",
          "amount_max",
          "owner"
        ],
        "filter": {
          "type": "buy"
        }
      },
      "display": {
        "component": "table"
      }
    },
    "full": {
      "title": "Full record",
      "description": "All fields including asset_name, asset_type, id, source_id, content_hash, and filing_type.",
      "transformation": {
        "fields": [
          "id",
          "politician",
          "transaction_date",
          "filing_date",
          "ticker",
          "asset_name",
          "asset_type",
          "type",
          "amount_min",
          "amount_max",
          "owner",
          "source_id",
          "content_hash",
          "filing_type"
        ]
      },
      "display": {
        "component": "table"
      }
    }
  }
}
